variable common_tags {
  type = map(string)
}

variable dev_tags {
  type = map(string)
}

variable common_subnets {
  type = list(string)
}

variable dev_subnets {
  type = list(string)
}

